info:
  name: Publish Post
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/posts"
  body:
    type: json
    data: |-
      {
        "content": "Hello world 4"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
