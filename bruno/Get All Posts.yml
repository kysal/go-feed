info:
  name: Get All Posts
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/posts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
